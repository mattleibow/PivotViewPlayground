<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <DefineConstants>$(DefineConstants);PIVOT_CORE</DefineConstants>
    <RootNamespace>Pivot</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\DeepZoom.Core\DeepZoom.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="..\Utils.Core\Shared\**" Link="%(RecursiveDir)%(Filename)%(Extension)" />
  </ItemGroup>

</Project>
